apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: store-healthcheck
spec:
  default:
    checkIntervalSec: 30
    timeoutSec: 30
    healthyThreshold: 2
    unhealthyThreshold: 5
    logConfig:
      enabled: false
    config:
      type: TCP
      httpHealthCheck:
        port: 8080
        requestPath: /
  targetRef:
    group: ""
    kind: Service
    name: store-v1